#!/usr/bin/env bash
# Search for files by exact filename.
#
# Usage: find-filename <pattern>
#
# Options:
#   -h    Show this help message

usage() {
    grep '^# ' "$0" | cut -c3-
    exit 1
}

flag=0
param=""

[ $# -eq 0 ] && usage

find . -type f -name "$@"
