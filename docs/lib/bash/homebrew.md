# Homebrew Module Reference

Helpers exposed by `lib/bash/homebrew` for working with Homebrew installations.

## `brew.prefix`
Detects the Homebrew installation prefix, caching the result in `_BREW_PREFIX`.

```bash
brew.prefix
```

```text
/opt/homebrew
```
